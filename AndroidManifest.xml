<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ==================== PERMISSIONS ==================== -->

    <!-- Storage permissions -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- For Android 13+ (API 33+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- Camera for document scanner -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!-- Internet for ML Kit OCR -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- ==================== APPLICATION ==================== -->

    <application
        android:name="com.propdf.editor.ProPDFApp"
        android:allowBackup="true"
        android:icon="@android:drawable/ic_menu_agenda"
        android:label="@string/app_name"
        android:roundIcon="@android:drawable/ic_menu_agenda"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProPDF"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="31">

        <!-- ==================== ACTIVITIES ==================== -->

        <!-- Main Activity -->
        <activity
            android:name="com.propdf.editor.ui.main.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ProPDF">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Intent filter for opening PDF files -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="application/pdf" />
                <data android:mimeType="application/octet-stream" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/pdf" />
            </intent-filter>
        </activity>

        <!-- PDF Viewer Activity -->
        <activity
            android:name="com.propdf.editor.ui.viewer.ViewerActivity"
            android:exported="false"
            android:theme="@style/Theme.ProPDF"
            android:configChanges="orientation|screenSize|keyboardHidden">
        </activity>

        <!-- Tools Activity -->
        <activity
            android:name="com.propdf.editor.ui.tools.ToolsActivity"
            android:exported="false"
            android:theme="@style/Theme.ProPDF"
            android:parentActivityName="com.propdf.editor.ui.main.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.propdf.editor.ui.main.MainActivity" />
        </activity>

        <!-- Document Scanner Activity -->
        <activity
            android:name="com.propdf.editor.ui.scanner.DocumentScannerActivity"
            android:exported="false"
            android:theme="@style/Theme.ProPDF"
            android:parentActivityName="com.propdf.editor.ui.main.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.propdf.editor.ui.main.MainActivity" />
        </activity>

        <!-- Settings Activity -->
        <activity
            android:name="com.propdf.editor.ui.settings.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.ProPDF"
            android:parentActivityName="com.propdf.editor.ui.main.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.propdf.editor.ui.main.MainActivity" />
        </activity>

        <!-- ==================== FILE PROVIDER ==================== -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.propdf.editor.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
